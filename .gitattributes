# Published data is generated by the pipeline (pnpm data:*): collapse it in reviews and
# keep it out of GitHub language statistics. `git diff --text` still shows it when needed.
public/data/** linguist-generated=true -diff
